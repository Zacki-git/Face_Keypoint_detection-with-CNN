采用项目二，stage1中的设置，用sgdm做优化器，做了两次计算。

第一次采用warmup策略，首先20轮lr==0.00001；然后80轮lr==0.0001；最后200轮lr==0.00001。测试和验证loss降到了20以内，不再下降，检测发现关键点不准确，说明并未收敛；

于是进行第二次计算，首先300轮lr==0.0001，然后100轮lr==0.00001。最终训练集loss可小于1，而验证loss在10以上，明显已过拟合；验证集最低时候降到了9.1，随着计算的增加训练集loss逐步减小，验证集loss增大；

准备在stage2中增加措施防止过拟合